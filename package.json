{
  "private": true,
  "name": "image-lazy-loading",
  "version": "1.0.0",
  "description": "A sample lazy loading & CDN worker for a Ghost Blog using Casper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Vortexmind <Vortexmind@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
